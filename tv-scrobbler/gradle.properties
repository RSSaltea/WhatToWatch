org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
kotlin.code.style=official

# Where the scrobbler reports to, and the shared secret. The token must match
# the DEVICE_TOKEN secret configured on the Cloudflare Pages project.
wtw.endpoint=https://whattowatch.uk/api/scrobble
wtw.token=CHANGE_ME_TO_A_LONG_RANDOM_STRING
